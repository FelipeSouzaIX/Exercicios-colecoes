import 'dart:io';

void main() {
  List<double> n = List.filled(100, 0);
  
  for (int i = 0; i < 100; i++) {
    n[i] = double.parse(stdin.readLineSync()!);
  }
  
  for (int i = 0; i < n.length; i++) {
    if (n[i] <= 10) {
      print('N[$i] = ${n[i]}');
    }
  }
}
